import mongoose from 'mongoose'; const s=new mongoose.Schema({name:String,price:Number,description:String,image:String,category:String,stock:Number}); export default mongoose.models.Product||mongoose.model('Product',s);